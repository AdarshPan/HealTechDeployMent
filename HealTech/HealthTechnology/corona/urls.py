
from django.conf.urls import url, include
from . import views
from django.conf import settings
from django.conf.urls.static import static
app_name="corona"
urlpatterns = [
    # url(r'survival_rate_prediction/',views.api_sentiment_pred, name="tp"),

    # url(r'l/',views.html,name="html_view"),
    # url(r'call/',views.call, name="call_model"),

]